# Dev dependencies for running the energy-stack test suite locally.
# Not installed in any service container — tests run on the operator's machine
# against the source.
#
# Install:
#     pip install -r deploy/energy-stack/requirements-dev.txt
#
# Plus each service's own runtime requirements.txt for its imports
# (pyControl4, aiohttp, influxdb-client, etc.).

pytest>=8
pytest-asyncio>=0.23
# Windows: ZoneInfo("America/Chicago") needs the iana database; container
# python:3.13-slim bundles it but Windows Python doesn't.
tzdata
