# Locked dependencies for the pre-registered analysis pipeline.
# See docs/ANALYSIS_PIPELINE.md §1 (Frozen environment).
#
# Before OSF lock, regenerate with hash pins:
#   pip-compile --generate-hashes requirements.in
# and commit. The shipped file in this PR uses minimum-version pins
# so the pipeline can be smoke-tested in CI without the full hash
# infrastructure.

numpy>=1.26,<2.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0
pyarrow>=15,<20
influxdb-client>=1.40,<2.0
pytest>=8.0,<9.0
